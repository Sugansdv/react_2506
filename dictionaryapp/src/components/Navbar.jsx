import React from 'react';

const Navbar = () => (
  <nav className="navbar navbar-dark bg-dark">
    <div className="container justify-content-center">
      <span className="navbar-brand mx-auto">📖 Dictionary App</span>
    </div>
  </nav>
);

export default Navbar;
